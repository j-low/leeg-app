# Core backend
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
pydantic[email]>=2.10,<3.0
pydantic-settings>=2.7,<3.0
python-dotenv>=1.0,<2.0

# Database
sqlalchemy[asyncio]>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
asyncpg>=0.30,<1.0
alembic>=1.14,<2.0

# Auth (also used by seed script for password hashing)
# bcrypt pinned <4.0 for passlib compatibility (passlib is unmaintained vs bcrypt 4.x)
passlib[bcrypt]>=1.7,<2.0
bcrypt>=3.2,<4.0
python-jose[cryptography]>=3.3,<4.0
python-multipart>=0.0.9,<1.0

# Rate limiting
slowapi>=0.1.9,<1.0

# SMS
twilio>=9.0,<10.0

# Async task queue
celery[redis]>=5.3,<6.0

# NLP (Stage 1 preprocessing)
# Llama Guard runs via Ollama (no torch/transformers needed)
spacy>=3.7,<4.0

# RAG (Stage 2: vector search, embeddings, compression)
qdrant-client>=1.9,<2.0
sentence-transformers>=3.0,<4.0
langchain-text-splitters>=0.3,<1.0
llmlingua>=0.2,<1.0

# Generation (Stage 3: LLM inference, tool calling, agentic loops)
anthropic>=0.40,<1.0          # Claude Haiku via Anthropic API
langgraph>=0.2,<1.0           # ReAct agent state graph
instructor>=1.4,<2.0          # Pydantic structured output (Anthropic provider)
jinja2>=3.1,<4.0              # Prompt template rendering

# HTTP client (async HTTP calls)
httpx>=0.27,<1.0

# Logging
structlog>=24.0,<26.0

# Testing (ahead of Phase 11 full test setup)
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
