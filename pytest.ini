[pytest]
asyncio_mode = auto
markers =
    unit: fast, isolated unit tests (no external services)
    integration: multi-component tests with mocked external dependencies
    e2e: full scenario tests through the HTTP layer
    load: locust performance tests (not collected by pytest)
testpaths = tests
